function fakeBin(x){
  let endString = ""
  for(let i = 0; i < x.length; i++){
    if(parseInt(x.substring(i,i+1)) < 5){
      endString = endString + "0"
    }else{
      endString = endString + "1"
    }
  }
  
  return endString
}
